package kakao.com.tess.group.service

class GroupService (

){

//    groupService.createGroup(userId, groupName): String { groupRepository.save(group), userGroupRole = (user, group, host), user.groupRoles.add(userGroupRole) }
//    groupService.inviteUser(hasToken, invitee, groupId) {token.validate, group.find, userGroupRole(invitee, group, participant), userGroupRoleRepository.add(userGroupRole) }
//    groupService.removeUser(hasToken, invitee, groupId) {token.validate, group.find, userGroupRole(invitee, group, participant), userGroupRoleRepository.remove(userGroupRole) }
//    groupService.changeUserRole(hasToken, inviter, invitee, groupId) {token.validate && inviter is host, userGroupRoleRepository.update(groupId, user,role)}

//    token.validate는 groupService에서는 AOP로 항상 ?
}